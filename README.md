# Knowledge Index / Индекс знаний

Персональный индекс знаний — репозиторий для структурированного хранения, организации и повторного использования накопленных знаний.

## Назначение

Этот репозиторий решает проблему разрозненности личных знаний, превращая заметки, конспекты, статьи и идеи в единый структурированный индекс с возможностью семантического поиска.

## Концепция

Система основана на [Концепции Индексатора знаний](https://github.com/aisystant/ecosystem-development/blob/main/0.OPS/0.9.Inbox/%D0%9A%D0%BE%D0%BD%D1%86%D0%B5%D0%BF%D1%86%D0%B8%D1%8F%20%D0%98%D0%BD%D0%B4%D0%B5%D0%BA%D1%81%D0%B0%D1%82%D0%BE%D1%80%D0%B0%20%D0%B7%D0%BD%D0%B0%D0%BD%D0%B8%D0%B9.md) и адаптирована для персонального использования.

### Принципы

- **Версионирование** — все знания хранятся в Git, что обеспечивает историю изменений
- **Нормализация** — контент разбивается на чанки (единицы знаний) с метаданными
- **Структурированность** — единый формат для всех типов контента
- **Переиспользуемость** — возможность находить и связывать знания между собой

## Структура репозитория

```
/
├── README.md
├── docs/              # Всё содержимое: заметки, эссе, посты (по годам)
│   ├── 2025/
│   ├── 2026/          # ← индексируются для MCP
│   └── ...
├── sources/           # Первичные источники
├── data/              # Данные
└── scripts/           # Скрипты обработки
```

### Организация контента (`docs/YYYY/`)

Весь контент (заметки, эссе, посты) хранится в `docs/` и организован по годам. Индексируются только `docs/2025/` и `docs/2026/`.

Новые публикации с мая 2026 года лежат в месячных папках:

```text
docs/{YYYY}/{NN}-{месяц}/{PP}-{MM}-{YYYY-MM-DD}-{slug}/
└── {PP}-{MM}-{channel_number}-{channel}-{YYYY-MM-DD}.md
```

`NN` — обратный номер месяца для сортировки свежих месяцев сверху, `MM` — календарный номер месяца, `PP` — порядковый номер публикации внутри месяца. Исторический сквозной номер хранится в поле `post_number`.

Имена задаёт общий `scripts/_publish_convention.json`: его читают серверный scaffold, локальный `scripts/new-post.py` и предкоммитная проверка через Python-адаптер. В конфигурации один реестр каналов, названия месяцев, правила и шаблоны имён. Отсутствующая или некорректная конфигурация блокирует создание и проверку; запасного набора имён нет. Месячный порядковый номер допускается от `01` до `99`, как в действующем формате.

В браузере вызовите `personal_new_post` с постоянным `draft_id`, `artifact_type: "post"` и объектом `scaffold`:

```json
{
  "date": "2026-09-25",
  "slug": "my-topic",
  "title": "Название поста",
  "audience": "community",
  "channels": ["club", "telegram"]
}
```

Обязательны `date`, `slug`, `title`; по умолчанию аудитория `community`, канал `club`. Дополнительно доступны `related_wp`, `content_plan`, `source_knowledge`. Сервер создаёт черновики и резервацию одним Git-коммитом и возвращает `scaffold.paths`; `club` всегда присутствует. Повтор того же UUID возвращает существующие пути, сохраняя текст. Для заполнения каждого файла прочитайте его через `personal_get_document(include_sha: true)` и передайте полученный SHA в `personal_write` как `expected_sha`; сохраняйте `draft_id` и `post_number`. Готовность к публикации и индекс в README обновляются отдельно после написания текста.

Локально сначала вызовите `personal_new_post` без `scaffold` для резервации номера, затем создайте файлы штатным скриптом:

```bash
python3 scripts/new-post.py --date YYYY-MM-DD --slug <slug> --title "<заголовок>" --channels club --post-number <номер> --dry-run
python3 scripts/new-post.py --date YYYY-MM-DD --slug <slug> --title "<заголовок>" --channels club --draft-id <UUID> --post-number <номер>
```

Сначала зарезервируйте сквозной номер через `personal_new_post` с постоянным UUID `draft_id`; при повторе используйте тот же UUID. Передайте UUID и полученный номер в скрипт. Через уже авторизованный `gh` он проверит уникальность этой пары в `docs/_allocator-log.jsonl` на снимке текущей основной ветки GitHub. Без подтверждённой резервации, доступа к GitHub или при повреждённом журнале файлы не создаются. Локального выделения номера и ручного обхода нет.

Скрипт сам вычисляет месячную нумерацию папок и проверяет локальные коллизии под блокировкой. Повтор UUID или номера сообщает путь существующего поста и останавливается, сохраняя написанный текст. `--dry-run` показывает только превью явно переданного номера, не проверяет и не создаёт резервацию.

На том же удалённом снимке скрипт проверяет занятость номера и UUID в исторических club-постах. Содержимое читается запросами GraphQL без изменений, пакетами до 40 неизменяемых Git-объектов. Поля берутся только из YAML-frontmatter; старое имя `NNN-1-club-...` служит запасным источником номера, месячное `PP-MM-1-club-...` — нет. Уже сохранённый пост блокирует создание даже для той же резервации. Неполное дерево, неполученный/обрезанный/двоичный файл и неоднозначный YAML останавливают запись. Лимиты полной проверки: 60 секунд, 20 000 записей дерева, 1000 club-файлов, 1 МиБ на файл и 8 МиБ суммарно.

Для записи нужны `gh` с авторизацией в GitHub и пакет `PyYAML` в используемом Python-окружении (`python3 -m pip install PyYAML`). Скрипт не устанавливает зависимости автоматически; отсутствие пакета прекращает работу до записи. `--dry-run` этой зависимости не требует.

Для исторических постов YAML `null` или пустое `post_number:` означают отсутствие глобального номера. Строка `"null"`, ноль и некорректные значения считаются ошибкой. Если указан `draft_id`, глобальный номер обязателен: иначе принадлежность резервации неоднозначна.

Серверный scaffold и локальное создание — два способа использовать один UUID. Если сервер уже создал файлы, локальный скрипт сообщает существующий путь вместо повторного создания. Две несинхронизированные локальные копии могут одновременно создать ещё не отправленный черновик одного UUID: локальная блокировка действует в пределах одной копии, поэтому этот случай требует синхронизации перед дальнейшей работой.

**Frontmatter для постов:**
```yaml
---
type: post
status: draft | ready | published
created: YYYY-MM-DD
target: club | facebook | linkedin | telegram | tenchat | x | youtube | dzen
post_number: 123  # сквозной номер из personal_new_post
draft_id: <UUID>  # постоянный идентификатор резервации
source_knowledge: <ссылка на Pack/Framework, откуда взято знание>
---
```

Посты — downstream-продукт. Знание берётся из Pack'ов и Framework'ов, пост — его адаптация для аудитории.

## Формат чанка

Каждая единица знаний оформляется как markdown-файл с YAML-метаданными:

```markdown
---
title: Название концепции
source: Источник (книга, статья, курс)
tags: [тег1, тег2, тег3]
created: 2026-01-18
updated: 2026-01-18
status: draft | active | archived
---

# Название

Содержание единицы знаний (до 10 000 символов).

## Связанные концепции

- [[Ссылка на другую концепцию]]
```

## Рабочий процесс

1. **Сбор** — новые материалы попадают в `0.Inbox/`
2. **Обработка** — материал изучается, выделяются ключевые идеи
3. **Нормализация** — создаются чанки с метаданными
4. **Размещение** — чанки распределяются по соответствующим разделам
5. **Связывание** — добавляются ссылки на связанные концепции

## Поиск и использование

Индекс поддерживает несколько способов поиска:

- **Навигация по структуре** — просмотр папок и файлов
- **Полнотекстовый поиск** — поиск по содержимому (через GitHub или локально)
- **Поиск по тегам** — фильтрация по меткам в метаданных
- **Семантический поиск** — поиск по смыслу (при наличии векторного индекса)

## Развитие

Планируемые этапы развития индекса:

1. **MVP** — ручное добавление и организация контента
2. **Автоматизация** — скрипты для импорта из различных источников
3. **Индексация** — создание векторных представлений для семантического поиска
4. **Интеграция** — подключение к AI-ассистентам для работы с базой знаний

## Лицензия

Личный репозиторий. Содержимое предназначено для персонального использования.

---

*Создан на основе методологии [Aisystant](https://github.com/aisystant)*
