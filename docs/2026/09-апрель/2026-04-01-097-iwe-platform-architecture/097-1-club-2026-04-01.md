---
type: post
title: "IWE как платформенный продукт: личное ядро знаний + MCP Gateway"
audience: advanced
status: draft
created: 2026-04-01
target: club
channel_number: 1
post_number: 97
source_knowledge: "PACK-digital-platform/DP.IWE.003"
tags: [iwe, mcp, архитектура, gateway, embeddings]
content_plan: "WP-73"
---

# IWE как платформенный продукт: личное ядро знаний + MCP Gateway

Последние несколько месяцев я проектировал IWE как персональную среду — для себя, руками, через VS Code и Claude Code. Теперь встал вопрос: как это превратить в продукт, который работает для тысяч людей, большинство из которых никогда не откроют терминал?

Это пост о том, какую архитектуру мы выбрали и почему именно такую.

---

## Проблема: два мира, которые не соединяются

Сегодня есть два полюса.

**Полюс 1: умные ИИ-ассистенты.** Claude, GPT, Gemini. Отвечают на вопросы, пишут код, суммируют документы. Но каждая сессия начинается с нуля. Ассистент не знает, чем ты занимаешься, что решил три месяца назад, какие у тебя принципы работы. Ты объясняешь контекст заново каждый раз.

**Полюс 2: системы хранения знаний.** Notion, Obsidian, Roam. Накапливают информацию, но пассивны. Ты ищешь сам, применяешь сам, связываешь сам. ИИ не знает, что там лежит.

Между этими полюсами — пропасть. И большинство людей существуют либо в одном, либо в другом, не имея ничего, что их соединяет.

IWE — это мост.

---

## Что такое IWE в этой архитектуре

IWE — не инструмент и не набор файлов. Это **конфигурация рабочей среды**: протоколы работы, подключения к сервисам, персональные знания, роли агентов.

Аналогия: IDE — это не твой код. Но без IDE код писать можно, а работать — нет. IWE — IDE для интеллектуальной работы.

В архитектурном смысле у пользователя есть три уровня:

```
Aisystant Platform
├── Знания МИМ (методология, системное мышление)
├── Embeddings личных Pack пользователей
├── Агенты платформы (Портной, Оценщик, Оркестратор)
├── IWE-шаблон (FMT-exocortex-template)
└── MCP Gateway ◄── авторизация через Ory
         │
         │ поставляет IWE + сервисы
         ▼
Личный IWE пользователя
├── Pack/DS репо (исходник личного знания, на GitHub)
└── Протоколы, настройки, роли
         │
         │ подключает Gateway
         ▼
Claude / GPT / любой вендорский агент
```

**Ключевое разделение:** данные (репо) живут у пользователя, вычислительное (embeddings, поиск, агенты) — на платформе.

---

## Что поставляет платформа

**Aisystant поставляет четыре вещи:**

**1. Знания МИМ.** Методология системного мышления — формализованная, структурированная, готовая к использованию в контексте агента. Не просто тексты — Pack-сущности с ID, frontmatter, связями.

**2. IWE-шаблон.** При регистрации платформа форкает `FMT-exocortex-template` в GitHub-пространство пользователя. Через OAuth, автоматически. Пользователь получает рабочую среду без настройки вручную.

**3. Embeddings личного Pack.** Пользователь работает в своём репо — пишет заметки, создаёт Pack-документы, фиксирует решения. Платформа векторизует эти файлы и хранит embeddings. При запросе агент ищет по смыслу, а не по ключевым словам.

**4. MCP Gateway.** Единая точка подключения. Вендорский агент (Claude, GPT, любой другой) подключается к одному URL. Gateway знает, кто этот пользователь, и отдаёт правильный контекст: знания МИМ + личный Pack + протоколы.

---

## Почему Gateway, а не прямое подключение

Это архитектурное решение, которое кажется очевидным только после того, как видишь альтернативу.

Альтернатива: каждый пользователь поднимает свой MCP-сервер. Это работает для технарей (я сам так и работаю через Claude Code CLI). Но для массовой аудитории это барьер.

Gateway решает проблему: один URL, авторизация через OAuth, никакой настройки на стороне пользователя. При этом внутри Gateway — три MCP-сервера:

- **knowledge-mcp** — публичные знания платформы (МИМ, гайды, CLAUDE.md пользователя)
- **digital-twin-mcp** — приватный цифровой двойник (данные о прогрессе, целях)
- **personal-knowledge-mcp** — личный Pack и embeddings

Разделение не случайное. Публичные знания кэшируются агрессивно, приватные — изолированы строго.

---

## Три технических решения, принятых сегодня

При проектировании мы прошли АрхГейт (оценку по семи характеристикам). Три ⚠️ потребовали явных решений.

**1. Переиндексация embeddings — webhook-триггер.**

Проблема: пользователь обновил Pack-репо → embeddings устарели → агент отвечает на старом контексте.

Решение: push в репо → GitHub webhook → задача в очередь → асинхронная частичная переиндексация. Пользователь не думает об индексе. Задержка — целевые ≤5 минут после push.

**2. Latency Gateway — SLA + кэш.**

Gateway добавляет hop между агентом и данными. Без ограничений это черный ящик.

Решение: SLA ≤200ms на Gateway overhead. Кэш контекста инвалидируется по commit hash репо — если Pack не менялся, отдаём из кэша без чтения файлов.

**3. Изоляция embeddings между пользователями — namespace per user.**

Векторное хранилище мультитенантное. Без явной изоляции поиск пользователя A может затронуть данные пользователя B.

Решение: фильтр `WHERE user_id = $1` в pgvector (Neon). `user_id` берётся из JWT-токена (Ory), не из параметра запроса — SQL-инъекция исключена по дизайну. Этого достаточно до ~30к пользователей. При росте — миграция на Qdrant с collection per user.

---

## Что это меняет для пользователя

Конкретный сценарий: человек регистрируется на Aisystant. Платформа создаёт ему репо на GitHub (форк шаблона IWE). Он начинает работать — в боте, на сайте, в Claude Code. Записывает заметки, создаёт планы, фиксирует решения.

Через год у него в репо — сотни файлов. Его принципы работы. Его доменные знания. Его история решений.

Теперь он подключает к этому репо GPT. Или Gemini. Или любой новый агент, который появится через два года. Gateway знает этого пользователя — и любой агент получает полный контекст.

Его IWE переносимо. Данные у него. Сервисы — у нас.

---

## Что дальше

Архитектура принята, решения зафиксированы в Pack. Следующий шаг — реализация personal-knowledge-mcp (WP-187): выделить L4-Personal из текущего knowledge-mcp, настроить webhook-переиндексацию, написать integration tests на изоляцию данных.

Если интересна техническая детализация — спрашивайте. Или смотрите [DP.IWE.003](https://github.com/TserenTserenov/PACK-digital-platform/blob/main/pack/digital-platform/02-domain-entities/DP.IWE.003-gateway-architecture.md) — там полная архитектурная спецификация.
